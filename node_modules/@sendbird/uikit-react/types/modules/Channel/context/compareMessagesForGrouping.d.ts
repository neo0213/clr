import { GroupChannel } from '@sendbird/chat/groupChannel';
import { ReplyType } from '../../../types';
import { CoreMessageType } from '../../../utils';
/**
 * exported, should be backward compatible
 */
export declare const compareMessagesForGrouping: (prevMessage: CoreMessageType, currMessage: CoreMessageType, nextMessage: CoreMessageType, currentChannel: GroupChannel, replyType: ReplyType) => [
    chainTop: boolean,
    chainBottom: boolean
];
