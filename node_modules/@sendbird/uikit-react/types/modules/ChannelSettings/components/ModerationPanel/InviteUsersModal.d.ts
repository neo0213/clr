import { ReactElement } from 'react';
interface Props {
    onCancel(): void;
    onSubmit(members: Array<string>): void;
}
export default function InviteUsers({ onCancel, onSubmit, }: Props): ReactElement;
export {};
