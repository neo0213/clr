import { BaseMessage } from '@sendbird/chat/message';
import { CustomUseReducerDispatcher, Logger } from '../../../../lib/SendbirdState';
import { ThreadListStateTypes } from '../../types';
import { SendableMessageType } from '../../../../utils';
interface DynamicProps {
    hasMorePrev: boolean;
    parentMessage: SendableMessageType;
    threadListState: ThreadListStateTypes;
    oldestMessageTimeStamp: number;
    isReactionEnabled?: boolean;
}
interface StaticProps {
    logger: Logger;
    threadDispatcher: CustomUseReducerDispatcher;
}
export default function useGetPrevThreadsCallback({ hasMorePrev, parentMessage, threadListState, oldestMessageTimeStamp, isReactionEnabled, }: DynamicProps, { logger, threadDispatcher, }: StaticProps): (callback?: (messages?: Array<BaseMessage>) => void) => void;
export {};
