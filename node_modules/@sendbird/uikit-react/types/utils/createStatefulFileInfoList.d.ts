import { MultipleFilesMessage } from '@sendbird/chat/message';
import { Thumbnail } from '@sendbird/chat/lib/__definition';
export type StatefulFileInfo = {
    fileName?: string;
    fileSize?: number;
    mimeType?: string;
    thumbnails?: Thumbnail[];
    url?: string;
    isUploaded?: boolean;
};
export declare function createStatefulFileInfoList(message: MultipleFilesMessage, oldStatefulFileInfoList: StatefulFileInfo[]): StatefulFileInfo[];
